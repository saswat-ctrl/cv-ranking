# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_to_secure_password
POSTGRES_DB=cv_ranking

# Backend Configuration
SECRET_KEY=change_this_to_a_long_random_secret_string
DOMAIN_NAME=yourdomain.com

# Backend CORS (optional override if needed, otherwise uses DOMAIN_NAME)
# BACKEND_CORS_ORIGINS=["http://yourdomain.com","https://yourdomain.com"]
